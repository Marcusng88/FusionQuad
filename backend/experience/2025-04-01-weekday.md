# End-of-Day Audit — 2025-04-01 (weekday)

## Overall Performance
- Total ticks: **8**
- Ticks within MD limit: **8/8** → **100% compliance**
- Total savings: **RM 0.00**
- Average shave per tick: **0.00 kW**
- BESS SOC: start **0.5000** → end **0.5900** (net delta **+0.0900**)
- Cycle count delta: **0.0**

## What Worked
- **All ticks stayed within MD limit**: tick 0 through tick 7 had `within_limit=true` (including PEAK windows at 14:00, 14:30, 15:00, 15:30, 16:00).
- **Safety constraints not violated**: peak-window charging/discharging commands never reduced SOC below the stated reserve thresholds (SOC increased from 0.50 to 0.595 by the final tick).

## What Failed or Was Suboptimal
- **No effective peak shaving / zero shave throughout the day**:
  - Peak window ticks: **tick 3 (14:00)**, **tick 4 (14:30)**, **tick 5 (15:00)**, **tick 6 (15:30)**, **tick 7 (16:00)** all resulted in either `action: charge` (with `actual_discharge_kw=50` due to log field inconsistencies) or `action_taken: hold` (tick 5) and recorded **savings_rm=0.0**.
  - Non-peak ticks (tick 0–2) also had **savings_rm=0.0**.
- **Dispatch/intent mismatch during PEAK**:
  - At **tick 3 (PEAK)** the planner set `dispatch_action.action = charge` with `charge_kw=50` while `dispatch_result.action_taken=charge` and `actual_discharge_kw=50`; this indicates a **directional semantic mismatch** in the log fields (charge vs discharge variables), which prevents computing meaningful shaving.
- **Forecast signal missing/unused**:
  - `forecast_kw` is **0.0 with confidence 0.0** for **all ticks**, while multiple strategies rely on forecast to compute `shave_kw` (and/or determine whether MD exceedance exists). As a result, the strategies defaulted to non-effective behavior and/or placeholder shave targets.
- **Forecast error signal (per delta evaluation)**:
  - Deterministic closing evaluation reported `forecast_error_pct=0.0` (but this is consistent with `forecast_kw=0` being non-informative rather than a real forecast).

## Recommendations for Next Similar Day
1. **Block or remap PEAK actions when forecast is missing**: if `forecast_confidence==0.0` or `forecast_kw==0` (or forecast list empty), prohibit issuing PEAK “shave” logic that leads to `savings_rm=0` outcomes; instead enforce a safe deterministic fallback that cannot invert charge/discharge semantics.
2. **Fix log/state semantics for power direction**: ensure that `dispatch_action` (`charge_kw`/`discharge_kw`) and `dispatch_result` (`actual_discharge_kw`, `action_taken`) use consistent definitions. Current log shows `action=charge` concurrent with `actual_discharge_kw=50`, which breaks downstream auditability and performance scoring.
3. **SOC policy for value extraction**: start PEAK windows with sufficient SOC headroom but do not charge during PEAK if the goal is demand shaving; add a rule such as: during `tariff_window=PEAK`, only allow charging if `md-exceedance indicator` is false and reserve for discharge exists. Otherwise prefer discharge/shave commands.

## Pattern Comparison (vs Past Experience)
- No prior files matching the same day_type were found in `/experience/` (no `*-weekday.md` or similar patterns). Therefore, trend comparison for forecast error/savings cannot be performed.
