# End-of-Day Audit — 2026-01-07 (holiday)

## Overall Performance
- Total ticks: **11**
- Ticks within MD limit: **0/11** → **0% compliance**
- Total savings: **RM 0.00**
- Average shave per tick: **0.00 kW**
- BESS SOC: start **0.5000** → end **0.9100** (net delta **+0.4100**)
- Cycle count delta: **0.0**

## What Worked
- Safety rules: closing-tick safety evaluation **passed** (no rule violations).
- SOC preservation/headroom: system consistently avoided discharging; SOC steadily increased from ~0.50 to ~0.91 (ticks 0–10 all “charge” actions).

## What Failed or Was Suboptimal
- No load shaving / savings throughout the run:
  - Every tick 0–10 had `peak_reduction_kw = 0.0` and `savings_rm = 0.0`.
  - No tick executed an effective discharge: the log shows `dispatch_action.action == "charge"` for all ticks; consequently `actual_discharge_kw = 0.0` everywhere.
- Tariff-direction mismatch (wrong direction) for PEAK:
  - **Not applicable in this log**: `tariff_window` is `WEEKEND` for all ticks; there are no `tariff_window == "PEAK"` ticks.
- Forecast error signal (closing tick):
  - `forecast_error_pct = 33.66%` (forecast 237.6514 kW vs actual 317.6514 kW) → forecast underpredicted actual load.
- MD-limit compliance logging/data quality:
  - Per-tick `within_limit` is **null** for all ticks; compliance is therefore only inferable from the provided summary fields (summary reports 0/11 peak-within-limit).

## Recommendations for Next Similar Day
1. **If tariff_window is WEEKEND/holiday and the strategy is “SOC preservation,” explicitly disable any charge/discharge optimization that can still increase grid import without savings**: cap/avoid `action: charge` when the objective has no MD/peak penalty driver (so the planner should prefer `hold` over sustained charging when shave is expected to remain 0).
2. **Add a forecast validation gate**: when `forecast_error_pct` magnitude exceeds a threshold (e.g., >10–15%) and actual_load threatens operational objectives, trigger a contingency action (e.g., conservative controlled discharge if SOC is high, or at minimum stop charging).
3. **Populate `within_limit` (and related peak fields) instead of null** so audits can compute compliance based on per-tick outcomes, not only summary placeholders.

## Pattern Comparison (vs Past Experience)
- Prior same-day-type experience exists: **2026-01-01 (holiday)**.
- Recurring pattern (same day_type):
  - 2026-01-01: run produced **0 savings** and **0% compliance**, with “no effective discharge/shaving” behavior.
  - 2026-01-07: same outcome (**0 savings**, **0% compliance**, average shave 0 kW). Additionally, SOC increased substantially (to 0.91) rather than staying flat.
- Forecast handling:
  - 2026-01-01 experience notes forecast/validation issues and MD logging nulls.
  - 2026-01-07 shows measurable underprediction at closing tick (**+33.66% forecast error**), suggesting the forecast signal validation/freshness remains a system-level concern.
- Trend assessment (evidence-based): **stable failure mode** (no shaving/savings) with ongoing data-quality gaps (`within_limit` null).
