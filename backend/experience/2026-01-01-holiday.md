# End-of-Day Audit — 2026-01-01 (holiday)

## Overall Performance
- Total ticks: **11**
- Ticks within MD limit: **0/11** → **0% compliance**
- Total savings: **RM 0.00**
- Average shave per tick: **0.00 kW**
- BESS SOC: start **0.3000** → end **0.3000** (net delta **+0.0000**)
- Cycle count delta: **0.0**

## What Worked
- No rule violations detected by the closing-tick safety evaluation (all dispatch actions respected configured constraints).
- SOC preservation succeeded throughout the run: every tick used `action: hold` with `new_soc: 0.3` (ticks 0–10).

## What Failed or Was Suboptimal
- Ineffective discharge / no shaving occurred across the entire day:
  - Ticks 0–10: `dispatch_action.action != discharge` (always `hold`), `savings_rm = 0.0`, `peak_reduction_kw = 0.0`.
- MD/peak compliance metric was not achieved:
  - Summary reports `peak_ticks=0` and `peak_compliance_rate=0.0`, but the log also shows no `within_limit` values per tick (field is null), so “MD-limit compliance” cannot be directly validated from per-tick data.
- Forecast error signal:
  - Closing-tick evaluation returned `forecast_error_pct = 0.0` with `forecast_kw == actual_load_kw == 66.75918` (tick 10 data available). No per-tick forecast-vs-actual error aggregation was available from the provided summary.

## Recommendations for Next Similar Day
1. **Add an explicit holiday/weekend behavior switch in the planner**: if tariff window is `WEEKEND/holiday`, confirm whether the goal is *pure SOC preservation* (zero shaving expected) or *MD limit avoidance* (in which case the strategy should still attempt controlled discharge/charge when SOC headroom exists).
2. **Fix forecast handling/validation**: the simulation sets `forecast_confidence = 0.0` for all ticks, and the planner holds at SOC floor. Add a gate that (a) checks forecast freshness/units and (b) triggers a fallback action (e.g., conservative shaving) when forecast confidence is 0 but actual load threatens operational objectives.
3. **Ensure MD compliance logging is populated**: `within_limit` is null for all ticks in the log. Populate `within_limit` (and/or peak-related fields) so audits can compute compliance meaningfully rather than relying on summary-only placeholders.

## Pattern Comparison (vs Past Experience)
- No prior same-day-type experience files were found in `/experience/` matching `*-holiday.md`, so no trend comparison is possible.
- Data quality note: per-tick `within_limit` is consistently `null`, limiting diagnosability of MD-limit behavior.
