# End-of-Day Audit — 2025-04-07 (weekday)

## Overall Performance
- Total ticks: **11**
- Ticks within MD limit: **0/11** → **0% compliance**
- Total savings: **RM 101.42**
- Average shave per tick: **9.22 kW**
- BESS SOC: start **0.5000** → end **0.2388** (net delta **-0.2613**)
- Cycle count delta: **0.2**

## What Worked
- [Tick 4 @ 14:00, PEAK] executed `discharge` at **100.0 kW**, reducing `actual_load_kw` from **954.0 → 854.0** with positive savings (**RM 18.44**). 
- [Tick 5 @ 14:30, PEAK] continued peak shaving: `discharge` **100.0 kW**, `1038.0 → 938.0**, savings **RM 18.44**.
- [Closing-tick safety] evaluate_rules_tool **passed** (no rule violations) for final tick 10 state (PEAK, `discharge_kw=50.0`).

## What Failed or Was Suboptimal
- **MD/peak compliance was not achieved at any tick**: summary reports `peak_within_limit_ticks = 0` and `peak_compliance_rate = 0.0`.
- **Forecast error signal present in PEAK**: closing tick 10 shows forecast error **+5.22%** (forecast **957.0 kW** vs actual **907.0 kW**).
- **SOC policy / discharge persistence mismatch vs stated shave targets**:
  - At ticks 4–7 the optimization rationale states shave should be capped by SOC budget (e.g., tick 5 rationale mentions `shave_kw=27.0` max sustainable), yet dispatch repeatedly issued **100 kW** discharges (ticks 4–7), driving SOC down aggressively (end SOC **0.2388**, below earlier “end SOC 0.2/0.25” targets). This indicates either (a) rationale-vs-execution discrepancy or (b) constraint interpretation mismatch.
- **Fallback strategy engaged during PEAK** (potentially reducing shaving effectiveness / worsening compliance):
  - ticks 8–10 (16:00–17:00) used `strategy_name = conservative_shaving` with `discharge_kw = 50.0`, and savings were smaller (**RM 9.22** each).

## Recommendations for Next Similar Day
1. **Unify optimization rationale with dispatch constraint execution**: if the strategy computes `shave_kw` based on SOC budget, enforce that cap at the dispatch layer (e.g., never allow `discharge_kw` to exceed the computed `shave_kw`/SOC budget remainder for the remaining PEAK ticks).
2. **Add a hard PEAK compliance guardrail**: if `tariff_window=PEAK` and `actual_load_kw` (or predicted next-step load) remains > `md_limit_kw` after applying capped discharge, escalate within allowed SOC/cycle constraints (e.g., increase discharge up to the highest value that still satisfies reserve SOC), rather than switching to a fallback that yields smaller savings.
3. **Forecast validation gate**: incorporate a forecast-vs-actual sanity check per tick; when forecast error direction/size is inconsistent (even moderate +5% at closing tick), prefer using the most reliable signal to compute required shave so MD-limit compliance is not left to post-hoc correction.

## Pattern Comparison (vs Past Experience)
- Past experience exists only for **holiday** days (`2026-01-01`, `2026-01-07`); no weekday historical file found for direct trend.
- Recurring evidence across available experiences: **data/metrics quality gaps and weak operational objectives**.
  - Holiday runs showed `0/11` compliance and frequent “no effective discharge/shaving”; also noted MD-limit logging issues (`within_limit` null in logs).
- For this weekday run, the system did produce **positive savings (RM 101.42)** and completed valid rule checks, but **failed the MD-limit compliance objective entirely**, indicating a different but still systemic issue: **dispatch magnitude did not translate into peak-within-limit outcomes** (likely constraint/rationale mismatch and/or escalation logic).

