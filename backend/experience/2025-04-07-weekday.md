# End-of-Day Audit — 2025-04-07 (weekday)

## Overall Performance
- Total ticks: **11**
- Ticks within MD limit: **{missing}** → **{missing}** compliance
- Total savings: **RM 58.86**
- Average shave per tick: **4.52 kW**
- BESS SOC: start **0.5238** → end **0.3959** (net delta **-0.1279**)
- Cycle count delta: **{missing}**

## What Worked
- PEAK window dispatch produced non-zero shaving and generated savings on multiple PEAK ticks, e.g. tick 4 (14:00, PEAK): actual_discharge_kw **43.44 kW**, savings **RM 8.01**, within_limit **false**; tick 6 (15:00): **57.49 kW**, savings **RM 10.60**; tick 10 (17:00, final tick): **43.29 kW**, savings **RM 7.98**.

## What Failed or Was Suboptimal
- MD limit compliance failed across PEAK ticks: summary reports **peak_within_limit_ticks = 0 / 7** (PEAK compliance **0%**).
- Ineffective discharge examples: no tick with `dispatch_action.action == "discharge"` and `savings_rm == 0` was observed in the provided ticks (all PEAK discharge ticks have positive savings).
- Direction mismatch: no ticks with `tariff_window == "PEAK"` and `dispatch_action.action == "charge"` were observed (PEAK actions were discharge-only in this log segment).
- Forecast error signal (tick 10 / final tick): forecast_kw **891.86 kW** vs actual_load_kw **913.71 kW** ⇒ forecast error **+2.45%** (under-forecasted actual load).
- System-level inconsistency: per-tick `within_limit` is frequently **false**, yet `evaluate_rules_tool` (final tick) returned **passed=true** with no rule violations; the deterministic rules engine appears to validate safety/constraints rather than MD-limit compliance.

## Recommendations for Next Similar Day
1. Add/strengthen an MD-compliance control: when `tariff_window=PEAK`, enforce a rule that scales discharge so that `actual_load_kw - peak_reduction_kw <= md_limit_kw` (or uses a closed-loop based on measured load, not just forecast exceedance). Current behavior shows discharge but still misses MD limit (PEAK compliance 0%).
2. Use measured-load feedback during PEAK: at each PEAK tick, compute `gap = actual_load_kw - md_limit_kw` and adjust `discharge_kw` proportionally for the next interval to reduce persistent `within_limit=false` outcomes.
3. SOC policy for discharge effectiveness: initial SOC is only **0.5238** and ends at **0.3959**; start PEAK with higher SOC (e.g., target SOC high enough to allow discharge sufficient to meet md_limit rather than only partially shaving). Without enough SOC headroom, discharge magnitude may be capped.

## Pattern Comparison (vs Past Experience)
- No prior weekday experience files were found in `/experience/` (only `.gitkeep`).
- Therefore, trend comparison (savings/compliance/forecast error) cannot be determined from historical audits.
