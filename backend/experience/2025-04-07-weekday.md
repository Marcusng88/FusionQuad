# End-of-Day Audit — 2025-04-07 (weekday)

## Overall Performance
- Total ticks: **11**
- Ticks within MD limit: **0/11** → **0% compliance**
- Total savings: **RM 129.08**
- Average shave per tick: **11.74 kW**
- BESS SOC: start **0.5380** → end **0.3195** (net delta **-0.2185**)
- Cycle count delta: **0.2500**

## What Worked
- PEAK discharge actions were executed consistently during **ticks 4–10 (PEAK)** with set discharge_kw **100.0 kW** at most ticks, producing positive interval savings each time.
  - Example: **tick 4 (14:00, PEAK)** baseline **954.0 kW** → actual **854.0 kW** with **100.0 kW** discharge; savings **RM 18.44**; within_limit=false.
  - Example: **tick 8 (16:00, PEAK)** baseline **1231.0 kW** → actual **1131.0 kW** with **100.0 kW** discharge; savings **RM 18.44**; within_limit=false.

## What Failed or Was Suboptimal
- **MD limit compliance failed entirely**: summary peak_within_limit_ticks **0** and peak_compliance_rate **0.0**; across all ticks, `within_limit` was **false** when evaluated.
  - Example: **tick 8 (16:00, PEAK)** actual load **1131.0 kW** still above **md_limit_kw=800** after discharge.
- **Dispatch setpoints were insufficient to drive grid import to the MD limit**, despite discharge actions.
  - Root cause (from log): PEAK strategy often applied **capped discharge** (frequent discharge_kw **100.0 kW**) even when forecasts/loads exceeded md_limit substantially (e.g., baseline **1231.0 kW** implies required shave > **331 kW** to reach 800 kW).
- **Fallback/strategy degradation at PEAK onset**:
  - **tick 10 (17:00, PEAK)** shows `optimization_strategy.strategy_name = conservative_shaving` with rationale **“Fallback strategy used due to missing or invalid planner output.”**
- **Forecast error signal (closing tick)**:
  - For **tick 10**, evaluate_delta reports forecast error magnitude **≈10.45%** (forecast **957.0 kW** vs actual **857.0 kW**). Direction: actual was lower than forecast.

## Recommendations for Next Similar Day
1. **MD-limit targeting in PEAK (remove/adjust discharge capping logic):** in PEAK, set discharge_kw to directly cover `(forecast_or_actual_load - md_limit_kw)` with an explicit guarantee that `actual_load_kw_after <= md_limit_kw` (subject to inverter/SOC constraints), rather than clamping to an interval cap that leaves large residual above 800 kW.
2. **Eliminate PEAK fallback during tariff transitions:** add a PEAK-window guard that prevents “missing/invalid planner output” from triggering `conservative_shaving`; when planner output is invalid, switch to a deterministic md-limit controller using the current tick’s load and md_limit.
3. **SOC policy for PEAK readiness:** current end SOC **0.3195** suggests SOC budgeting was aggressive; raise OFF_PEAK `target_soc_end` so that the battery can sustain md-limit-compliant discharge for more than the initial PEAK ticks.

## Pattern Comparison (vs Past Experience)
- Past same day_type experience files matching `*-weekday.md` were not available before this write (directory contained only the current file). Therefore, trend comparison is not reliable from prior entries.
