# End-of-Day Audit — 2025-04-07 (weekday)

## Overall Performance
- Total ticks: **9**
- Ticks within MD limit: **2/9** → **22% compliance**
- Total savings: **RM 110.64**
- Average shave per tick: **0.00 kW**
- BESS SOC: start **0.5000** → end **0.2100** (net delta **-0.2900**)
- Cycle count delta: **0.2**

## What Worked
- Peak-window shaving direction was issued when `tariff_window=PEAK` and `dispatch_action.action=discharge` (ticks **2–7**): discharge commands ranged `discharge_kw=100 kW`, with non-zero savings recorded (**RM 18.44**) for ticks **2–7**.
- Safety rule compliance at closing tick: deterministic evaluation `evaluate_rules_tool` **passed** on the final tick state.

## What Failed or Was Suboptimal
- MD limit violations dominated: `within_limit=false` for ticks **2,3,4,5,6,7,8** (7 of 9). Example: **tick 2 (14:00, PEAK)** had actual load **854 kW** (vs MD limit 800 kW) with savings **RM 18.44**, but still outside limit.
- Ineffective discharge at end of day: **tick 8 (17:00, PEAK)** issued `dispatch_action.action=hold` with `actual_discharge_kw=0.0` and **savings_rm=0.0**, while actual load was still high (**957 kW**).
- Forecast/actual mismatch signal exists in the closing evaluation: `evaluate_delta_tool` reported **forecast_error_pct ≈ 7.30%** for the final tick interval.

## Recommendations for Next Similar Day
1. **Prevent late PEAK “hold” when load stays above MD limit:** add a hard rule: if `tariff_window=PEAK` and `actual_load_kw` (or conservative forecast exceedance) indicates expected import > 800 kW, disallow `hold` and require discharge/shave until `within_limit=true` is achieved.
2. **Cap discharge by SOC reserve but schedule earlier/larger effective shaving:** current behavior discharged at the 100 kW interval cap while MD-limit compliance remained low (2/9). Adjust control to start shaving at the first PEAK tick and ensure enough effective kW is available across consecutive PEAK intervals.
3. **Calibrate the decision buffer relative to observed forecast error:** `evaluate_delta_tool` forecast error at closing tick was ~**7.30%**; re-tune the PEAK override buffer logic (the “+15 kW” requirement) so requested shave more closely matches observed exceedance.

## Pattern Comparison (vs Past Experience)
- Compared to **2025-04-01 (weekday)**: previous day had **8/8** compliance but recorded **zero realized savings** across ticks (primary issue: forecast features missing/unused and/or semantic mismatches).
- Compared to **previous stored 2025-04-07 weekday experience entry** (this file prior version): both runs show dominant PEAK MD-limit non-compliance; this run ends with lower SOC (end **0.21** vs previously recorded **0.32**), consistent with more sustained discharge earlier and less ability to maintain shaving later.
- Forecast error signal exists now: closing `evaluate_delta_tool` reports **~7.30%** forecast error, indicating forecast is usable, but shave magnitude remains insufficient to enforce MD limit.
